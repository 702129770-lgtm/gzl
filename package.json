{
  "name": "canva-ppt-workflow-console",
  "version": "1.0.0",
  "private": true,
  "description": "Canva PPT workflow console with Feishu app bot notifications.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
